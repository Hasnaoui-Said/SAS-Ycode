#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main(){
    int randomnumber = 0;
	srand(time(NULL));
    //int nRandonNumber = Rand()%((nMax+1)-nMin) + nMin;
    printf("%d\n", nRandonNumber);
    return 0;
}
